# Proxy inverso con HTTPS automático (Let's Encrypt) delante de la app.
# El dominio se inyecta desde la variable AUTH_DOMAIN del .env.
{$DOMINIO} {
	encode zstd gzip
	# Subidas de fotos de hasta 12 MB
	request_body {
		max_size 12MB
	}
	reverse_proxy app:3000
}
